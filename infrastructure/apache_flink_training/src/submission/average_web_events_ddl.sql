-- Define a table to store the average number of web events in a session by hostname (average_web_events_ddl)


-- Define a table to store the average number of web events in a session by hostname (average_web_events_ddl)

CREATE TABLE average_web_events_ddl (
    dim_host_name VARCHAR,
    m_avg_num_web_events FLOAT
)


